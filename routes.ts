// Public
export const ROOT = "/";
export const PUBLIC_ROUTES = [
    "/",
    "/about",
    "/contact",
    "/faq"
];

export const DEFAULT_REDIRECT_ROUTE = "/check-in";

// Authentication
export const AUTH_ROUTES = [
    "/auth/sign-in",
    "/auth/sign-up",
    "/auth/forgot-password",
    "/auth/reset-password",
    "/auth/verified-email"
];

// Admin
export const DEFAULT_REDIRECT_ROUTE_ADMIN = "/dashboard";

export const API_AUTH_PREFIX = "/api/auth"